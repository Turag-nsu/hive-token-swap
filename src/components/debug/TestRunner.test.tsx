import { TestRunner } from './TestRunner';

// Simple test to verify the TestRunner component can be imported
describe('TestRunner', () => {
  it('should be defined', () => {
    expect(TestRunner).toBeDefined();
  });
});